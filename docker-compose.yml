services:
  app:
    build:
      context: .
    command: php artisan serve --host=0.0.0.0 --port=8000
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"              
    networks:
      - finance-project-network
    depends_on:
      - db
      - redis

  reverb:
    build:
      context: .
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    ports:
      - "8080:8080"
    networks:
      - finance-project-network
    depends_on:
      - redis

  horizon:
    build:
      context: .
    command: php artisan horizon
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    networks:
      - finance-project-network
    depends_on:
      - db
      - redis

  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: FinanceProject
      POSTGRES_USER: root
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - finance-project-network

  redis:
    image: redis:alpine
    networks:
      - finance-project-network
    volumes:
      - redisdata:/data 

  evolution-api:
    container_name: evolution_api
    image: evoapicloud/evolution-api:v2.3.7
    restart: always
    ports:
      - "8081:8080"
    env_file:
      - .env
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - finance-project-network
    depends_on:
      - db
      - redis

networks:
  finance-project-network:

volumes:
  pgdata:
  redisdata:
  evolution_instances: